package testProject;

public class main {
	public void Foo() {
		int x, y, a, i, j, k, z;
		if (x + y) { a = i; } else { Foo(); }
		if (p) { a = j; } else { Foo(); }
		if (q) { z = k; } else { Bar(); }
	}
	public void Bar() {} 
}
